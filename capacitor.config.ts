import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jahzeel.dev',
  appName: 'policia-cientifica',
  webDir: 'dist/policia-cientifica',
  
};

export default config;
